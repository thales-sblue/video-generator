[processing]
# Media processing remains local. Internet research and authorized asset
# acquisition are orchestrator concerns, not remote processing fallbacks.
local_only = true
allow_external_services = false
preserve_sources = true

[paths]
inputs = "inputs"
assets = "assets"
projects = "projects"
output = "output"
